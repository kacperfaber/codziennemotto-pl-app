import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.codziennemotto',
  appName: 'codziennemotto.pl',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
